<%= render "header" %>

<%if current_user%>
 <%if  Role.find(@role.roles_id).roles_name == "Admin"%>
  <%= render "scheduleList"%>
 <%elsif Role.find(@role.roles_id).roles_name == "User"%>
  <%= render "userschedule"%>
 <%end%>
<%else%>
   <%=render "userbook"%>
<%end%>