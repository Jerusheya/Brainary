<%= render "header" %>
<%if current_user%>
  <%if  Role.find(@role.roles_id).roles_name == "Admin"%>
  <%= render "donatelist_showing"%>
  <%elsif Role.find(@role.roles_id).roles_name == "User"%>
  <%= render "userdonate_books"%>
  <%end%>
<%else%>
   <%=render "userdonate_books"%>
<%end%>